services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./db:/app/db
      - ./images:/app/images:ro
      - ./videos:/app/videos:ro
      - ./data:/app/data:ro
    restart: unless-stopped
